# Ralph Progress Log
Started: Mon Aug 10 15:25:03 CEST 2026
---

## Codebase Patterns
- The npm workspaces are ordered shared, CLI, then web so production builds make shared package artifacts available to downstream packages.
- Development type checks resolve `@codevis/shared` directly to its TypeScript source; CLI production builds resolve the already-built declaration output.
- Solidity relationship resolution should use compiler AST declaration IDs and normalized source-unit paths rather than matching symbol labels.
- CLI commands should keep validated JSON on stdout and route progress plus source-linked compiler diagnostics to stderr.
- The web client loads validated graph data from `/api/graph`; development and production smoke tests use the static fixture at that path until the watch service owns the endpoint.
- Progressive graph visibility should follow `contains` ancestry, with files initially expanded for architecture and contracts initially collapsed; filter edges to visible endpoints whenever nodes are hidden.
- Source inspection reads repository-relative file contents from the validated graph's `metadata.sources` map and uses node line/column ranges for excerpts and highlights.
- Graph filtering should be applied after progressive containment visibility, while search explicitly expands all `contains` ancestors before selecting and focusing a hidden result; security presets may bypass collapsed detail but should retain visible hierarchy.

## 2026-08-10 16:16 CEST - US-001
- What was implemented: Created the npm/TypeScript workspace with shared graph, Node CLI/local-service, and React/Vite web packages; added root scripts and development documentation.
- Files changed: Added root npm/TypeScript configuration, README, ignore rules, workspace source/tests/configuration, lockfile, and updated PRD/progress tracking.
- Quality checks run: `npm run typecheck`, `npm test`, `npm run build`, production artifact checks, and a live `npm run dev` HTTP smoke check all passed.
- Learnings for future iterations: Keep shared first in the root workspace list; Vitest needs an explicit shared-source alias before the shared production package has been built.
---

## 2026-08-10 16:18 CEST - US-002
- What was implemented: Defined the versioned unified graph schema, all required node/edge kinds and statuses, source locations, JSON metadata, deterministic source-based node IDs, actionable runtime validation, and validated serialization.
- Files changed: Updated `packages/shared/src/index.ts`, expanded `packages/shared/src/index.test.ts`, and updated PRD/progress tracking.
- Quality checks run: `npm run typecheck`, `npm test` (8 workspace tests passed), `npm run build`, and `git diff --check` all passed.
- Learnings for future iterations: Use `createStableNodeId` for source-derived node identities and pass untrusted or deserialized graphs through `parseGraph` before consumers use them.
---

## 2026-08-10 16:20 CEST - US-003
- What was implemented: Added a documented Foundry fixture with imported Solidity files, related contracts, inheritance, public/internal functions, a modifier, state reads/writes, events, custom errors, external and value-sending calls, plus passing and intentionally failing tests; added an automated fixture availability check.
- Files changed: Added `packages/cli/test/fixtures/solidity-project`, expanded `packages/cli/src/index.test.ts`, and updated PRD/progress tracking.
- Quality checks run: `npm run typecheck`, `npm test` (9 workspace tests passed), `npm run build`, `forge build`, and `git diff --check` all passed.
- Learnings for future iterations: Reuse `packages/cli/test/fixtures/solidity-project` for Solidity analyzer and Foundry integration tests; the intentionally failing Foundry test is expected fixture behavior.
---

## 2026-08-10 16:23 CEST - US-004
- What was implemented: Added deterministic recursive Solidity source discovery for absolute and relative project directories, default and configurable ignore handling, normalized repository-relative results, and actionable errors for inaccessible, invalid, unreadable, and empty projects.
- Files changed: Added `packages/cli/src/discover-solidity-files.ts`, exported the discovery API from `packages/cli/src/index.ts`, expanded CLI unit tests, and updated PRD/progress tracking.
- Quality checks run: `npm run typecheck`, `npm test` (12 workspace tests passed), `npm run build`, and `git diff --check` all passed.
- Learnings for future iterations: Reuse `discoverSolidityFiles` as the source-file entry point for analysis and watch mode so both features share deterministic paths and ignore behavior.
---

## 2026-08-10 16:27 CEST - US-005
- What was implemented: Added Solidity compiler AST analysis that extracts deterministic file, contract, function, modifier, event, custom-error, and state-variable nodes with structural metadata, byte-accurate source ranges, and preserved compiler diagnostics.
- Files changed: Added `packages/cli/src/analyze-solidity-structure.ts`, exported and tested the analyzer, added `solc`, extended shared node kinds for errors and state variables, and updated PRD/progress tracking.
- Quality checks run: `npm run typecheck`, `npm test` (14 workspace tests passed), `npm run build`, and `git diff --check` all passed.
- Learnings for future iterations: Reuse `analyzeSolidityStructure` and its compiler AST output path for relationship and behavior analysis; Solidity `src` offsets are UTF-8 byte offsets and should remain byte-based in graph source locations.
---

## 2026-08-10 16:35 CEST - US-006
- What was implemented: Added deterministic repository, directory, file, and contract hierarchy plus resolved Solidity import and inheritance edges, duplicate-edge suppression, and coverage for unresolved relationship diagnostics.
- Files changed: Updated `packages/cli/src/analyze-solidity-structure.ts`, expanded `packages/cli/src/index.test.ts`, and updated PRD/progress tracking.
- Quality checks run: `npm run typecheck`, `npm test` (15 workspace tests passed), `npm run build`, and `git diff --check` all passed.
- Learnings for future iterations: Resolve Solidity declarations through AST numeric IDs and imports through compiler-normalized absolute paths; compiler diagnostics remain the source of actionable unresolved import and inheritance errors.
---

## 2026-08-10 16:38 CEST - US-007
- What was implemented: Added AST-backed function call, state read/write, and modifier relationships; added external-call and value-sending function metadata; retained unresolved low-level calls as source-linked informational diagnostics.
- Files changed: Updated `packages/cli/src/analyze-solidity-structure.ts`, expanded `packages/cli/src/index.test.ts`, and updated PRD/progress tracking.
- Quality checks run: `npm run typecheck`, `npm test` (16 workspace tests passed), `npm run build`, and `git diff --check` all passed.
- Learnings for future iterations: Traverse expression ancestry to classify compound state assignments as both reads and writes, and resolve behavior relationships exclusively through compiler declaration IDs; preserve unresolved low-level calls as evidence metadata and diagnostics.
---

## 2026-08-10 16:41 CEST - US-008
- What was implemented: Added the `codevis analyze [path]` shell command with current-directory defaults, validated JSON output to stdout or a requested file, stderr-only progress and source-linked diagnostics, documented help, and reliable exit codes for input and compiler failures.
- Files changed: Added `packages/cli/src/run-cli.ts`, updated the CLI entry point and integration coverage, and updated PRD/progress tracking.
- Quality checks run: `npm run typecheck`, `npm test` (19 workspace tests passed), `npm run build`, `git diff --check`, executable `--help`, and a built CLI fixture-analysis smoke test all passed.
- Learnings for future iterations: Keep stdout reserved for machine-readable graph payloads; use the exported `runCli` boundary for deterministic command tests while smoke-testing the built executable for bin wiring.
---

## 2026-08-10 16:46 CEST - US-009
- What was implemented: Added a React Flow repository canvas that fetches and validates `/api/graph`, deterministically lays out hierarchy, distinguishes node and relationship kinds, supports pan/zoom/fit/reset, and presents polished loading, empty, and analysis-error states; added a representative development graph fixture.
- Files changed: Rebuilt the web application UI and styles, added graph rendering and fixture data, added Vitest component tests and Playwright browser coverage, updated web dependencies/configuration, ignore rules, PRD, and progress tracking.
- Quality checks run: `npm run typecheck`, `npm test` (22 workspace tests passed), `npm run build`, `npm run test:browser --workspace @codevis/web` (Chromium smoke test passed), and `git diff --check` all passed.
- Learnings for future iterations: Keep `/api/graph` as the validated web/service boundary; React Flow nodes need measured container dimensions in DOM tests, while real viewport behavior remains covered by the Playwright smoke test.
---

## 2026-08-10 16:50 CEST - US-010
- What was implemented: Added stable file and contract expansion state, a function-level collapsed initial view, visible-endpoint edge filtering, accessible expansion controls, and a fit-to-expanded-groups viewport action.
- Files changed: Updated the repository graph component and styling, expanded component and Chromium smoke coverage, and updated PRD/progress tracking.
- Quality checks run: `npm run typecheck`, `npm test` (23 workspace tests passed), `npm run build`, `npm run test:browser --workspace @codevis/web` (Chromium progressive-detail smoke test passed), and `git diff --check` all passed.
- Learnings for future iterations: Derive progressive visibility from `contains` ancestors and filter relationships only after calculating the visible node set so no edge references a hidden endpoint.
---

## 2026-08-10 18:18 CEST - US-011
- What was implemented: Added node selection and a responsive evidence panel with kind, status, metadata, incoming/outgoing relationships, repository-relative locations, highlighted line-numbered source excerpts, copy-location support, and explicit handling for source-less nodes.
- Files changed: Updated the repository graph component and styling, enriched the development graph with source evidence, expanded component tests, and updated PRD/progress tracking.
- Quality checks run: `npm run typecheck`, `npm test` (25 workspace tests passed), `npm run build`, `npm run test:browser --workspace @codevis/web` (Chromium smoke test passed), and `git diff --check` all passed.
- Learnings for future iterations: Keep source contents keyed by normalized repository-relative paths in `graph.metadata.sources`; source positions use exclusive end columns, which makes precise inline highlighting deterministic.
---

## 2026-08-10 18:21 CEST - US-012
- What was implemented: Added case-insensitive node label and repository-path search with ancestor reveal, selection, and viewport focus; independent node-kind and relationship-kind toggles; a security evidence preset for external calls, state writes, value-related functions, and findings; and full filter reset behavior.
- Files changed: Updated the repository graph component and responsive filter styling, expanded component and Chromium browser coverage, and updated PRD/progress tracking.
- Quality checks run: `npm run typecheck`, `npm test` (28 workspace tests passed), `npm run build`, `npm run test:browser --workspace @codevis/web` (2 Chromium smoke tests passed), and `git diff --check` all passed.
- Learnings for future iterations: Apply user filters after progressive graph visibility; search results should expand containment ancestors before viewport focus, while evidence presets can intentionally reveal matching collapsed nodes together with their hierarchy.
---
